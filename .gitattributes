# Keep LF in the working tree on every platform. Prettier is configured with
# endOfLine: "lf", so a CRLF checkout on Windows would fail `format:check`.
* text=auto eol=lf

package-lock.json -diff linguist-generated
