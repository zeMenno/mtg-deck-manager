/** Maximum saved versions retained per deck (Phase 11). */
export const MAX_VERSIONS_PER_DECK = 50;

/** Snapshot schema version for forward-compatible restores. */
export const SNAPSHOT_VERSION = 1 as const;
