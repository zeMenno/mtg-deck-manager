/**
 * DeckCard types — re-exported from `types/deck` for the Phase 5 file map.
 */
export type {
  DeckCard,
  DeckCardWithCard,
  DeckCardSnapshot,
} from "@/types/deck";

export type { DeckCardStatus, DeckCardZone } from "@/types/index";
